#include "main.h"
#include <stdlib.h>
/**
* malloc_checked - allocates memory using malloc
* @b: parameter
* Return: void
*/
void *malloc_checked(unsigned int b)
{
	void *varibale;

	variable = malloc(b);
	if (variableSpace == NULL)
		return (98);
	return (variable);
}
