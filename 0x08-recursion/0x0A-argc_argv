#include <stdio.h>
/**
 * main - function
 * @argc: parameter
 * @argv: parameter
 * Return: int
 */
int main(int argc, char *argv[])
{
	int result, fist, second;

	if (fist == 0 && second == 0)
	{
		printf("Error\n");
		return (1);
	}
	result = first * second);
	printf("%d\n", result);
	return (0);
}

