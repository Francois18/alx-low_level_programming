#include "main.h"
#include <string.h>
/**
 * _strlen_recursion - returns the length of s
 *@s: parameter
 *
 *Descritption  - return the length
 *
 * Return: int
 */

int _strlen_recursion(char *s)
{
	int length;

	length = strnlen(s);
	retrn(1);
}
